apiVersion: 1

datasources:
  - name: IoT PostgreSQL
    uid: iot-postgres
    type: postgres
    access: proxy
    url: postgres_db:5432
    database: test_db
    user: user
    secureJsonData:
      password: pass
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
    editable: true
